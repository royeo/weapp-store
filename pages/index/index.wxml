<view>
  <map
    id="storeMap"
    longitude="{{longitude}}"
    latitude="{{latitude}}"
    scale="{{scale}}"
    controls="{{controls}}"
    bindcontroltap="controltap"
    markers="{{markers}}"
    bindregionchange="regionchange"
    show-location>
  </map>
</view>